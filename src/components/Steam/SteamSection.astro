---
import SteamCard from "./SteamCard.astro";
import { fetchSteamData } from "../../lib/steam";

const data = await fetchSteamData();
const games = data.games;
const totalPlaytime = data.totalPlaytime;
---

<section aria-label="Steam activity" class="flex flex-col gap-2">
  <div class="border border-border bg-card p-4 sm:p-5">
    <div class="mb-3 flex items-center justify-between border-b border-border pb-2">
      <p class="text-xs font-medium uppercase tracking-wider text-text-muted">steam activity</p>
      <p class="text-sm text-text-secondary">{totalPlaytime} hrs on record</p>
    </div>

    {games.length > 0 ? (
      <div class="flex flex-col gap-2">
        {games.map((game) => (
          <SteamCard
            name={game.name}
            playtimeHours={game.playtimeHours}
            lastPlayed={game.lastPlayed}
            imageUrl={game.imageUrl}
            appid={game.appid}
          />
        ))}
      </div>
    ) : (
      <div class="py-8 text-center text-sm text-text-secondary">
        <p>No recent Steam activity</p>
        <p class="mt-1 text-xs text-text-muted">Play any game on Steam to see it here</p>
      </div>
    )}
  </div>
</section>